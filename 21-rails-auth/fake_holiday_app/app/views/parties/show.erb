<h1>A Party for <%= @party.holiday.name %></h1>
<h3><%= @party.date %></h3>
<em><%= @party.location %></em>
<p><%= "🤠" * @party.headcount %></p>

<% if @current_user == @party.user %>
  
  <%= button_to "Edit", edit_party_path(@party), method: :get %>
  <%= button_to "Delete", @party, method: :delete %>

<% end %>
